let frutas = ['sandia','melon','manzana','cereza']

// Retorna el indice de cada uno de los valores del array

for (let frutakey in frutas) {
    console.log(frutakey)
}
    
// Retorna los valores del array
for (let frutavalue of frutas) {
    console.log(frutavalue)
}